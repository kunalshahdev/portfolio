{
  "name": "portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && ./node_modules/.bin/minify index.html > dist/index.html && ./node_modules/.bin/minify style.css > dist/style.css && ./node_modules/.bin/minify index.js > dist/index.js",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "minify": "^11.0.0"
  }
}
